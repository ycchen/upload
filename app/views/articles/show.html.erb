<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
	<% @article.photos.each do |photo| %>
		<%= image_tag(photo.file.url, :heigh => 200, :width => 200) %>
	<% end %>
</p>
<br/>
<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
